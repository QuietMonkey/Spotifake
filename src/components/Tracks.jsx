import React from 'react'
import Track from './Track';

const Tracks = (name) => {

    // const renderingTracks = () => {
    //     data.map((track) => <Track title='test'/>)
    // }


    return(
        <div>
            <h2>{name}</h2>
        </div>
    )
}

export default Tracks